# Desabilita cache do servidor LiteSpeed (Hostinger)
<IfModule LiteSpeed>
  CacheLookup off
</IfModule>

# Desabilita cache do servidor Apache
<IfModule mod_cache.c>
  CacheDisable /
</IfModule>

<IfModule mod_headers.c>

  # HTML, JS e CSS — sempre revalida com o servidor
  # Se o arquivo mudou: baixa o novo. Se não mudou: usa cache (304, rápido).
  # Resultado: toda publicação aparece imediatamente para todos os visitantes.
  <FilesMatch "\.(html|htm|js|css)$">
    Header set Cache-Control "no-cache, must-revalidate"
    Header set Pragma "no-cache"
  </FilesMatch>

  # Imagens — cache de 7 dias (mudam raramente)
  <FilesMatch "\.(png|jpg|jpeg|gif|svg|ico|webp|avif)$">
    Header set Cache-Control "public, max-age=604800"
  </FilesMatch>

  # Fontes — cache de 1 ano (nunca mudam)
  <FilesMatch "\.(woff|woff2|ttf|eot)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

</IfModule>

# Compressão gzip
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript text/plain
</IfModule>
